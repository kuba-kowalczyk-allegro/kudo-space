SUPABASE_URL=###
SUPABASE_KEY=### # Anon key from the test project

# OAuth Configuration
# Supported providers: github
SUPABASE_OAUTH_PROVIDER=github

# Site Configuration
# Used for OAuth redirects (e.g., http://localhost:3000 for local dev or https://your-domain.com for production)
SITE_URL=http://localhost:4321

# OpenRouter Configuration
OPENROUTER_API_KEY=###
# OPENROUTER_API_URL=https://openrouter.ai/api/v1 (optional, defaults to this)
# OPENROUTER_DEFAULT_MODEL=openai/gpt-4o-mini (optional, defaults to meta-llama/llama-3.3-8b-instruct:free)
# OPENROUTER_TIMEOUT_MS=30000 (optional, defaults to 30000)

# GitHub OAuth credentials (configured in Supabase dashboard)
GITHUB_CLIENT_ID=your-actual-client-id
GITHUB_CLIENT_SECRET=your-actual-client-secret